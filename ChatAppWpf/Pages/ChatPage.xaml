<Page x:Class="ChatAppWpf.Pages.ChatPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ChatAppWpf.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ChatPage"
      d:DataContext="{d:DesignInstance Type=local:ChatPage}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="64"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="64"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="chatTextBlock" Grid.ColumnSpan="2" Margin="16"></TextBlock>

        <TextBox Grid.Row="1" Margin="16" x:Name="messageTextBox"></TextBox>
        <Button Grid.Row="1" Grid.Column="1" Margin="16" Content=">" Click="SendButtonClick"></Button>
    </Grid>
</Page>
